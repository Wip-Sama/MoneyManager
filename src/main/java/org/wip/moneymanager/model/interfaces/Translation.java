package org.wip.moneymanager.model.interfaces;

public interface Translation {
    String translate(String key);
    // ve l'ho messa per metterne almeno una nel progetto ma non serve praticamente a niente
    // Ho un po' di difficoltà a capire quando qualcosa dovrebbe avere un'interfaccia
    // Credo di aver capito il concetto di Interfaccia ma non abbiamo casi in cui
    // diverse implemntazioni devo corrispondere allo stesso set di azioni
}
